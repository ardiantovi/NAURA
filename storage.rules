
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read access to everyone to display images publicly.
    match /{allPaths=**} {
      allow read;
      allow write: if request.auth != null;
    }
  }
}
